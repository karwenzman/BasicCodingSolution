{
  "ApplicationInformation": {
    "LastLogin": "today (stored in <appsettings.Production.json> file)",
    "Language": "en-US (stored in <appsettings.Production.json> file)"
  },
  "ConnectionStrings": {
    "Default": "Default connection string (stored in <appsettings.Production.json> file)"
  }
}